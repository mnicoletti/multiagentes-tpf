# Config del JUDGE — DISTINTA de config/models.yaml de los agentes (ADR-0007).
# El harness carga SOLO este archivo vía get_judge_model(); nunca reusa instancias
# de cartera/mercado/planificador/redactor.
#
# Demo Anthropic: Sonnet t=0 (SPEC §9). Profesor Google:
#   PORTFOLIOSENTINEL_JUDGE_MODELS_YAML=evals/judge/models.gemini.yaml
# Ollama (dev): models.ollama.yaml de agentes ≠ este archivo; ver models.gemini.yaml.

roles:
  judge:
    provider: anthropic
    model: claude-sonnet-4-5-20250929
    params:
      temperature: 0
